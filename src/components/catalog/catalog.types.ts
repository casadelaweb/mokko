export interface catalogElements {
  layout: HTMLElement | null
}

export interface selectors {
  slider: string,
  controls: string,
  card: string,
  buttons: {
    prev: string,
    next: string,
  },
}
